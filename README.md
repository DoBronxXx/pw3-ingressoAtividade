# pw3-ingresso
//Gustavo Alberico e Luigih Ragauske

CREATE TABLE TBL_SESSAO(
ID BIGINT PRIMARY KEY IDENTITY,
DATA DATE,
HORARIO DATETIME,
CONSTRAINT FK_SESSAO_FILME FOREIGN KEY (ID_FILME) REFERENCES FILME(ID_FILME),
PRECO FLOAT,
SALA VARCHAR(20)
);
